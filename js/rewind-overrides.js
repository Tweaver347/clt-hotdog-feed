const style = document.createElement("style");
style.textContent = `
  .rewind-photo-copy,
  .rewind-ending-actions {
    position: relative;
    z-index: 25;
  }
`;
document.head.appendChild(style);
